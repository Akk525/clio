#!/bin/bash

# API Live Testing Script
# Tests all API endpoints with the Next.js dev server running
# Run with: ./scripts/test-api-live.sh

echo ""
echo "╔══════════════════════════════════════════════════════════════════╗"
echo "║              🌐 TESTING API ROUTES (LIVE) 🌐                     ║"
echo "╚══════════════════════════════════════════════════════════════════╝"
echo ""

# Colors
GREEN='\033[0;32m'
BLUE='\033[0;34m'
YELLOW='\033[1;33m'
NC='\033[0m'

# Check if server is running
if ! curl -s http://localhost:3000 > /dev/null 2>&1; then
    echo -e "${YELLOW}⚠️  Next.js dev server not running${NC}"
    echo "Please start it with: npm run dev"
    echo ""
    exit 1
fi

echo -e "${GREEN}✅ Next.js server is running${NC}"
echo ""

# Test 1: Stats
echo "─────────────────────────────────────────────────────────────────"
echo -e "${BLUE}TEST 1: GET /api/stats${NC}"
echo "─────────────────────────────────────────────────────────────────"
curl -s http://localhost:3000/api/stats | jq '.'
echo ""

# Test 2: Leaderboard
echo "─────────────────────────────────────────────────────────────────"
echo -e "${BLUE}TEST 2: GET /api/leaderboard?limit=3${NC}"
echo "─────────────────────────────────────────────────────────────────"
curl -s "http://localhost:3000/api/leaderboard?limit=3" | jq '.'
echo ""

# Test 3: User Badges
echo "─────────────────────────────────────────────────────────────────"
echo -e "${BLUE}TEST 3: GET /api/profile/[address]/badges${NC}"
echo "─────────────────────────────────────────────────────────────────"
USER="0xmuselover1234567890123456789012345678901"
echo "Testing with user: $USER"
curl -s "http://localhost:3000/api/profile/$USER/badges" | jq '. | length as $len | "Total badges: \($len)"'
curl -s "http://localhost:3000/api/profile/$USER/badges" | jq '.[0]'
echo ""

# Test 4: Full Profile
echo "─────────────────────────────────────────────────────────────────"
echo -e "${BLUE}TEST 4: GET /api/profile/[address]${NC}"
echo "─────────────────────────────────────────────────────────────────"
curl -s "http://localhost:3000/api/profile/$USER" | jq '.'
echo ""

# Test 5: Artist Supporters
echo "─────────────────────────────────────────────────────────────────"
echo -e "${BLUE}TEST 5: GET /api/artists/100/supporters${NC}"
echo "─────────────────────────────────────────────────────────────────"
curl -s "http://localhost:3000/api/artists/100/supporters" | jq '. | length as $len | "Total supporters: \($len)"'
curl -s "http://localhost:3000/api/artists/100/supporters" | jq '.[0]'
echo ""

# Test Error Handling
echo "─────────────────────────────────────────────────────────────────"
echo -e "${BLUE}TEST 6: Error Handling${NC}"
echo "─────────────────────────────────────────────────────────────────"
echo "Testing invalid address..."
curl -s "http://localhost:3000/api/profile/invalid/badges" | jq '.'
echo ""
echo "Testing non-existent artist..."
curl -s "http://localhost:3000/api/artists/99999/supporters" | jq '.'
echo ""

echo "═══════════════════════════════════════════════════════════════════"
echo -e "${GREEN}✅ ALL API ROUTES WORKING!${NC}"
echo "═══════════════════════════════════════════════════════════════════"
echo ""

